### Calcul du pathloss avecv les formules 3GPP
# On considere seulement les scenario RMa, UMa et UMi

# -------------------------------------------------
#     RMa LOS ET nlOS
# -------------------------------------------------

def _rma_nlosp(d_2D, d_Bp, h_Bs, H_UT, W, h):
    if  d_2D>= && d_2D <= dB
    PL_RMa = PL1
    
    return 

def rma_los():

    return 


# -------------------------------------------------
#     UMa LOS ET nLOS
# -------------------------------------------------

def uma_los():
    
    def _uma_los_pl1(d_2D, d_Bp, h_UT, H_BS, fichier_de_cas, fichier_de_device, antenna_id, ue_id, antennas, ues):
        d2D = calculate_distance(coord1, coord2)
        d3D_out =  
        d3D_in = 
        D3D = sqrt()
        if d2
        P_Luma_Los  = PL_1

        return 

    def _uma_los_pl2(arg1, arg2, etc):
        return pl
    
# -------------------------------------------------
#     UMI  LOS ET nLOS
# -------------------------------------------------

def umi_los():
    d_2D, d_Bp, h_UT, H_BS,
    def _uma_los_pl1(fichier_de_cas, fichier_de_device, antenna_id, ue_id, antennas, ues):
        d2D = calculate_distance(coord1, coord2)
        d3D_out =  
        d3D_in = 
        D3D = sqrt()
        if d2
        P_Luma_Los  = PL_1

        return 

    def _uma_los_pl2(arg1, arg2, etc):
        return pl